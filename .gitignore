__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
# root-anchored: only the top-level runtime cache dir, NOT gameday/data/
/data/
*.duckdb

# artifacts are regenerable — except the small demo fixtures the dashboard
# falls back to on a fresh clone (live latest_* artifacts are machine-local,
# so a deployed clone's worktree never goes dirty from a pipeline run)
artifacts/*
!artifacts/forecasts
artifacts/forecasts/*
!artifacts/forecasts/demo
!artifacts/forecasts/demo/**
.env
.DS_Store

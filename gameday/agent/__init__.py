"""The draft agent: Gameday's artifacts exposed as tools, plus the prompt that
teaches the ranking methodology behind them.

Import this package only where the agent is actually used — it pulls in
`claude_agent_sdk`, which is an optional dependency (`pip install -e '.[agent]'`).
"""
